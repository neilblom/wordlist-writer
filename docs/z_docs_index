Docs Index — WordList Writer
Version: 2026-07-20
Status: Authoritative Source of Truth

Overview
This index lists every document in the /docs folder and explains its purpose, scope, and when future-you should reference it. It is the entry point for understanding the entire project. All documents are restart-proof and aligned with the updated July 19 architecture.

1. PRD.md — Product Requirements Document
Purpose:
* defines product vision
* lists functional and non-functional requirements
* describes core features, UI layout, and curriculum logic
* establishes long-term goals and constraints
Use When:
* restarting the project after a long break
* clarifying what the app is supposed to do
* ensuring new features align with original goals

2. Roadmap.md
Purpose:
* defines development phases
* lists completed work and upcoming milestones
* shows dependency order (English-only -> Supabase -> Multilingual)
Use When:
* planning next development steps
* checking what phase you are in
* ensuring work follows correct sequence

3. Tech_Stack.md
Purpose:
* defines frontend, backend, and database technologies
* documents architecture choices
* explains how static JSON, Express, and Supabase interact
Use When:
* rebuilding environment after a long break
* onboarding a developer
* verifying architectural consistency

4. Frontend_Architecture.md
Purpose:
* defines single-layer editor architecture
* documents highlight pipeline, tokenizer, rendering rules
* explains IME-safe input handling and startup sequence
Use When:
* debugging highlight issues
* rebuilding editor logic
* extending frontend functionality

5. Backend_Architecture.md
Purpose:
* defines Express server structure
* documents API routes, controllers, and Supabase integration
* explains save/load pipelines and backend invariants
Use When:
* debugging backend issues
* adding new API endpoints
* rebuilding backend logic

6. Rendering_Pipeline.md
Purpose:
* defines how tokens become HTML spans
* documents highlight rendering rules
* explains project list and violations rendering
Use When:
* debugging rendering issues
* rebuilding highlight logic
* extending UI rendering behavior

7. Master_List_Workflow.md
Purpose:
* defines curriculum sequence logic
* documents master list editing rules
* explains cognate insertion and typed-word workflows
Use When:
* debugging curriculum-order issues
* extending master list features
* onboarding teachers or developers

8. Developer_Workflow.md
Purpose:
* defines how to work on the codebase
* documents pipelines, architecture rules, and coding conventions
* explains save/load pipeline and project ID rules
Use When:
* starting a new development session
* onboarding a developer
* reconstructing internal architecture

9. Teacher_Workflow.md
Purpose:
* defines teacher-facing usage of the app
* documents highlight colors, cognate workflow, violations panel
* explains curriculum-aligned writing workflow
Use When:
* onboarding instructors
* designing teacher-facing features
* validating pedagogical behavior

10. Data_Sources.md
Purpose:
* lists all linguistic data sources (frequency, lemma, cognates)
* defines where each file lives
* documents normalization and tokenization rules
Use When:
* adding new languages
* updating frequency lists
* debugging lemma or cognate behavior

11. Decisions.md
Purpose:
* records all architectural and technical decisions
* includes reasoning behind each decision
* prevents repeated debates or regressions
Use When:
* unsure why something was designed a certain way
* evaluating whether to change a system
* reconstructing historical context

12. Detailed_Feature_Specifications.md
Purpose:
* defines internal behavior of all major systems
* tokenizer, highlight pipeline, violations panel
* master list, project list, editor layers
* frequency ingestion rules
Use When:
* implementing or debugging core features
* rebuilding highlight logic
* extending curriculum or cognate systems

13. Supabase_Schema.md
Purpose:
* defines all tables, fields, constraints, and relationships
* documents normalization rules and mapping logic
* establishes backend data model
Use When:
* implementing backend routes
* debugging save/load pipeline
* rebuilding database after a long break

14. API_Specification.md
Purpose:
* defines all backend API routes, request bodies, and responses
* documents ID propagation and saveEverything contract
* establishes backend <-> frontend communication rules
Use When:
* implementing or debugging backend
* rebuilding save/load pipeline
* adding new API endpoints

15. Cognate_Dictionary.md
Purpose:
* defines cognate data structures (official, pending, merged)
* documents dictionary profiles and hybrid cognate window
* explains pending -> official merging workflow
Use When:
* debugging cognate behavior
* extending cognate system
* adding new languages or profiles

16. Setup_Supabase.md
Purpose:
* defines step-by-step Supabase setup
* documents environment variables, table creation, connection tests
* explains save/load verification
Use When:
* setting up backend for first time
* rebuilding Supabase project
* debugging connection issues

17. Next_Steps.md
Purpose:
* lists immediate, short-term, medium-term, and long-term tasks
* defines Phase 1-5 implementation steps
* documents backend debugging tasks
Use When:
* starting a new coding session
* planning next day’s work
* tracking progress across phases

18. Glossary.md
Purpose:
* defines all project terminology
* ensures consistent vocabulary across docs and code
Use When:
* reviewing unfamiliar terms
* onboarding a developer
* clarifying linguistic or architectural concepts

Summary
This index provides a complete map of the WordList Writer documentation set. Each file is restart-proof and designed to reconstruct the entire project quickly and accurately. Future updates must keep this index aligned with the /docs folder.

Documentation Formatting Reminder
* use plain text section titles
* use asterisks (*) for bullet points
* do not insert blank lines inside bullet lists
* use ASCII-only characters
* avoid Markdown headings (#)
* avoid fenced code blocks unless necessary
* use Step format for workflows to prevent GitHub auto-renumbering
