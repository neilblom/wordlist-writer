Developer Workflow
Version: 2026-07-17
Status: Authoritative Developer Guide

Overview
This document explains how to work on the WordList Writer codebase. It is designed for future developers and future sessions that need to reconstruct the internal architecture, pipelines, and coding conventions. It follows the GitHub-safe formatting rules used across the project.

1. Architecture Overview
WordList Writer consists of four major layers:
* UI Layer (HTML, CSS, JS)
* Analysis Layer (tokenizer, normalization, highlight pipeline)
* Curriculum Layer (Master List, Project List, Cognates, Frequency)
* Backend Layer (Express plus Supabase)

The system uses static JSON files for linguistic data, a lightweight Express server, and Supabase for persistence.

2. Folder Structure
The project uses the following folder layout:
* src/controllers
* src/lib
* src/routes
* src/utils
* public/frequency
* public/lemmas
* public/cognates
* public/master
* public/master/backups
* docs
* scripts

Folder purposes:
* controllers: backend logic for API routes
* lib: shared backend utilities such as Supabase client and normalization helpers
* routes: Express route definitions
* utils: frontend utilities such as tokenizer and highlight logic
* public: static JSON data and client-side assets
* docs: full documentation suite
* scripts: maintenance scripts

3. Frontend Pipeline Overview
The frontend pipeline processes text in the following order:
* tokenize text
* normalize lemmas
* lookup cognates
* lookup frequency
* lookup curriculum rank
* determine highlight color
* update project list
* update violations panel
* render UI

All steps must remain in this order.

4. Tokenizer Workflow
Tokenizer responsibilities:
* split text into tokens
* preserve punctuation
* handle contractions
* return array of token objects

Location:
src/utils/tokenizer.js

5. Normalization Workflow
Normalization ensures consistent lemma handling.
Rules:
* lowercase
* Unicode NFD
* strip diacritics
* remove punctuation

Location:
src/utils/normalizeLemma.js

6. Highlight Pipeline
Highlighting uses priority rules:
* cognate: green
* known: black
* unknown: red

Location:
src/utils/highlight.js

Dependencies:
* masterSet
* frequencyList
* cognateMap
* tokenizer
* normalizeLemma

7. Project List Workflow
Project List tracks all lemmas in the current text.
Rules:
* normalized lemmas only
* no duplicates
* update dynamically as text changes

Location:
src/utils/projectList.js

8. Master List Workflow
Master List defines curriculum order.
Rules:
* unique lemmas
* unique ranks
* normalized lemmas
* cross-language equivalents allowed

Location:
src/utils/masterList.js

9. Violations Workflow
Violations detect curriculum issues.
Types:
* unknown word
* out-of-order word
* curriculum gap

Location:
src/utils/violations.js

10. Backend Architecture
Backend uses Express.
Responsibilities:
* serve static files
* expose API routes
* connect to Supabase
* handle save and load pipeline

Location:
src/server.js

11. API Routes
API routes are defined in:
src/routes

Controllers live in:
src/controllers

Full specification:
See docs/API_Specification.md

12. Supabase Integration
Supabase stores:
* projects
* project_wordlists
* master_wordlists
* cross_language_master

Supabase client lives in:
src/lib/supabase.js

Full schema:
See docs/Supabase_Schema.md

13. SaveEverything Workflow
SaveEverything must:
* save project
* capture returned id
* save project wordlist
* save master list
* update UI

This order must never change.

14. Load Workflow
Load workflow must:
* load project metadata
* load project wordlist
* load master list
* rebuild UI state
* re-run highlight pipeline

15. Debugging Workflow
Common debugging steps:
* check normalizeLemma
* check tokenizer output
* check masterSet
* check frequencyList
* check cognateMap
* check highlight pipeline
* check violations panel
* check save and load pipeline

16. Adding New Features
When adding new features:
* update Decisions.md
* update Detailed_Feature_Specifications.md
* update API_Specification.md if backend changes
* update Supabase_Schema.md if schema changes
* update README.md if user-facing changes
* update Docs_Index.md

17. Safe Coding Practices
* never modify masterList directly; use helper functions
* always normalize lemmas before insertion
* always update masterSet after changes
* always re-run highlight pipeline after edits
* always validate ranks
* always guard against null values

18. Summary
This developer workflow explains how to work on the WordList Writer codebase, how pipelines interact, how the backend integrates with Supabase, and how to safely add new features. It is designed for future developers and future sessions.

Documentation Formatting Reminder
* Use plain text section titles
* Use asterisks (*) for bullet points
* No blank lines inside bullet lists
* ASCII-only characters
* Avoid Markdown headings
* Avoid fenced code blocks unless necessary
* Use Step format for workflows
